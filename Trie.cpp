#include <algorithm>
#include <vector>

using namespace std;

struct trie
{
	vector<int> go[];

	trie(vector<string>& arr)
	{
		go[0].resize(n)
		for (auto s : arr)
		{
			int x = 0;
			for (int i = 0; i < s.size(); ++i)
			{
				if (go[x][s[i]])
			}
		}
	}
};